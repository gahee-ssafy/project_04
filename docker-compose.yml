services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    env_file:
      - ./backend/.env
    environment:
      - DB_PATH=/data/app.db

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
