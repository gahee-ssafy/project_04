
# =====================================================
# requirements.txt
# =====================================================

# --- 웹 프레임워크 ---
fastapi==0.115.0          # 비동기 API 프레임워크
uvicorn[standard]==0.30.0 # ASGI 서버 (uvloop + httptools 포함)

# --- 데이터베이스 ---
sqlmodel==0.0.21          # SQLAlchemy + Pydantic 통합 ORM
aiosqlite==0.20.0         # SQLite 비동기 드라이버

# --- AI / LLM ---
google-generativeai==0.7.2  # Gemini API SDK

# --- HTTP 클라이언트 ---
httpx==0.27.0             # 비동기 HTTP 요청 (Serper API 호출용)

# --- 유틸리티 ---
python-dotenv==1.0.1      # .env 파일 환경변수 로드
